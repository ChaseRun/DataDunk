#!/bin/bash
# install monog and twitter

# stop on errors
set -Eeuo pipefail
set -x

# setup virtual enviroment
python3 -m venv env
source env/bin/activate
pip install pymongo[srv]

pip install -e mongo
pip install -e twitter
